This module allows to use server environment with fs storage. It is a
bridge between fs_storage and server_environment modules. It provides an
implementation of the StorageEnvironment class that uses the filesystem
as a storage backend.
