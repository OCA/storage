Consider this module when using `fs_attachment`, and kanban views relying on the
Avatar Mixin, such as contacts, are slow, AND the usual workaround of storing
small images in the database does not work for you.
